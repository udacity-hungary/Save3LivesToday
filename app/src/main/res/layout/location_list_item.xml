<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/location_list_item_height"
    android:padding="16dp">

    <!-- TODO replace placeholder text with tools:text -->
    <TextView
        android:id="@+id/tv_location_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Saint Ann Center"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/tv_location_address"
        app:layout_constraintEnd_toStartOf="@id/iv_location_icon"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="spread" />

    <!-- TODO replace placeholder text with tools:text -->
    <TextView
        android:id="@id/tv_location_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Budapest, Thököly út 14."
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/iv_location_icon"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_location_name" />

    <!-- TODO replace placeholder image with tools:src -->
    <ImageView
        android:id="@+id/iv_location_icon"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:src="@drawable/placeholder_location"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1" />

</android.support.constraint.ConstraintLayout>