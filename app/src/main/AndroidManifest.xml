<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.intellicode.savethreelivestoday">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.map.MapActivity" />
        <activity android:name=".ui.onboard.OnBoardActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.user.UserActivity" />
        <activity android:name=".ui.user.UserDetailsActivity" />
        <activity android:name=".ui.info.InfoActivity" />
        <activity android:name=".ui.statistics.StatisticsActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB7HlSSKVlYCVLD_f4RnctFEso4TzX44uw" />

    </application>

</manifest>